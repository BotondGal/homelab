apiVersion: v1
kind: Service
metadata:
  name: the-node
  annotations:
    tailscale.com/tailnet-fqdn: "the-node.tailea8881.ts.net"
    tailscale.com/proxy-group: "egress-proxies"
spec:
  type: ExternalName
  externalName: placeholder
  ports:
    - port: 8000
      protocol: TCP
      name: llm
    - port: 8001
      protocol: TCP
      name: embedding
