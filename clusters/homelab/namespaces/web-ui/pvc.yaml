apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openwebui-data
  labels:
    app: openwebui
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
