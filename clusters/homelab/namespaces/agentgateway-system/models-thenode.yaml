---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayModel
metadata:
  name: the-node-gemma4-12b
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: agentgateway-chat
      sectionName: http
  provider: OpenAI
  baseURL: http://the-node.gateway.svc.cluster.local:8000/v1
  match:
    model: the-node/gemma4-12b
  policies:
    auth:
      secretRef:
        name: the-node
